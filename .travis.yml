language: ruby
rvm:
  - "2.4.3"
services:
  - mysql
env:
  - DB=mysql
before_script:
  - cp config/database.travis.yml config/database.yml
  - bundle exec rake db:create
  - export DISPLAY=:99.0

  

